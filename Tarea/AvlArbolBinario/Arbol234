class Nodo234:
    def __init__(self):
        self.valores = []  # puede tener hasta 3 valores
        self.hijos = []


class Arbol234:
    def __init__(self):
        self.raiz = Nodo234()


    # Inserción iterativa
    def insertar(self, valor):
        nodo = self.raiz

        while True:

            # Si nodo lleno → dividir
            if len(nodo.valores) == 3:
                self.dividir(nodo)

            # Si es hoja
            if not nodo.hijos:
                nodo.valores.append(valor)
                nodo.valores.sort()
                return

            # Bajar al hijo correcto
            if valor < nodo.valores[0]:
                nodo = nodo.hijos[0]
            elif len(nodo.valores) == 1 or valor < nodo.valores[1]:
                nodo = nodo.hijos[1]
            else:
                nodo = nodo.hijos[-1]


    # División de nodo
    def dividir(self, nodo):
        medio = nodo.valores[1]
        # lógica simplificada de división